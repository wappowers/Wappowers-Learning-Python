from random import randint

board = []

# Creates board
for x in range(5):
    board.append(["O"] * 5)

# Makes the board print in a prettier format, without commas.
def print_board(board):
    for row in board:
        print " ".join(row)

print "Let's play Battleship!"
print_board(board)

# Functions to create random locations
def random_row(board):
    return randint(0, len(board) - 1)

def random_col(board):
    return randint(0, len(board[0]) - 1)

# Places ship
ship_row = random_row(board)
ship_col = random_col(board)

#Print's ship location, to help with debugging
print ship_row
print ship_col

# The bulk of the logic is below
turn_number = 0
#Limits the user to 4 guesses
for turn in range(4):
    guess_row = int(raw_input("Guess Row:"))
    guess_col = int(raw_input("Guess Col:"))

    if guess_row == ship_row and guess_col == ship_col:
        print "Congratulations! You sunk my battleship!"
        break
    else:
        if (guess_row < 0 or guess_row > 4) or (guess_col < 0 or guess_col > 4):
            print "Oops, that's not even in the ocean."
        elif(board[guess_row][guess_col] == "X"):
            print "You guessed that one already."
        else:
            print "You missed my battleship!"
            # Places an "X" in the location of their miss
            board[guess_row][guess_col] = "X"
            turn_number += 1
            print "Turn", turn_number + 1
            print_board(board)
        if turn_number == 3:
            print "Game Over"
